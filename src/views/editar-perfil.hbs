<main class="contenedor">
    <form action="/editar-perfil" method="POST" class="default-form" enctype="multipart/form-data">
        <div class="campo">
            <label>Nombre</label>
            <input type="text" name="nombre" value="{{usuario.nombre}}" placeholder="Nombre">
        </div>
        <div class="campo">
            <label>E-mail</label>
            <input type="email" name="email" value="{{usuario.email}}" placeholder="E-mail">
        </div>
        <div class="campo">
            <label>Password</label>
            <input type="password" name="password" placeholder="Contraseña">
        </div>
        <div class="campo">
            <label>Imagen</label>
            <input type="file" name="imagen">
            <img src="/uploads/perfiles/{{imagen}}" class="admin-perfil" alt="">
        </div>
        <div class="campo">
            <input type="submit" class="btn btn-azul" value="Guardar Cambios">
        </div>
    </form>
</main>